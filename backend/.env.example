# Backend environment variables
# For local development, point MONGO_URI at your MongoDB host.
# When running with Docker Compose, docker-compose.yml already injects the correct values.

MONGO_URI=mongodb://localhost:27017/ilfs
JWT_SECRET=your_jwt_secret_here
PORT=5000
